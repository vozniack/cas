export enum ScopeType {
  INTERNAL = 'INTERNAL',
  EXTERNAL = 'EXTERNAL'
}

export enum ViewType {
  TABLE = 'TABLE',
  LIST = 'LIST',
  GRID = 'GRID',
  TREE = 'TREE'
}

export enum ResourceType {
  ORGANIZATION = 'ORGANIZATION',
  USER = 'USER',
  ROLE = 'ROLE',
  PRIVILEGE = 'PRIVILEGE'
}
